{
  "name": "project-parcel-module",
  "version": "1.0.0",
  "description": "parcel module",
  "source": "src/index.ts",
  "main": "dist/main.js",
  "type": "module",
  "types": "dist/types.d.ts",
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build",
    "examples": "parcel examples/index.html --https --open --port 8080",
    "test": "web-test-runner src/**/*.spec.ts --node-resolve --esbuild-target auto",
    "test:watch": "web-test-runner src/**/*.spec.ts --node-resolve --esbuild-target auto --watch",
    "cov": "web-test-runner src/**/*.spec.ts --node-resolve --esbuild-target auto --coverage",
    "cov:watch": "web-test-runner src/**/*.spec.ts --node-resolve --esbuild-target auto --coverage --watch"
  },
  "keywords": [],
  "author": "stagas",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.12.0",
    "@esm-bundle/chai": "^4.3.4-fix.0",
    "@parcel/config-default": "^2.0.0",
    "@parcel/validator-typescript": "^2.0.0",
    "@web/dev-server-esbuild": "^0.2.14",
    "@web/test-runner": "^0.13.20",
    "parcel": "^2.0.0"
  }
}
